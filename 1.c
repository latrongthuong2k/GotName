#include <stdio.h>

struct no1 
{
public: 
     int x, y;
};

int main ()
{
    printf("Get x = "); scanf("%d", &x);
    printf("Get y = "); scanf("%d", &y);
    printf("x = %d, y = %d", x, y);
    return 0;
}
